{% extends 'base.html' %}
{% block title %}{{ job.title }} at {{ job.company }} — TechSphere{% endblock %}
{% block content %}

<div style="max-width: 700px; margin: 0 auto;">
  <a href="{% url 'job_list' %}" style="font-size: 12px; color: var(--text-3); text-decoration: none; display: inline-block; margin-bottom: 16px;" onmouseover="this.style.color='var(--accent)'" onmouseout="this.style.color='var(--text-3)'">← Back to Job Board</a>

  <div class="card" style="padding: 24px; margin-bottom: 16px;">
    <div style="display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 16px;">
      <div style="display: flex; align-items: center; gap: 12px;">
        <div class="avatar avatar-lg" style="background: var(--accent-light); color: var(--accent-text); font-size: 20px; font-weight: 700; border-radius: 12px;">{{ job.company|slice:":1"|upper }}</div>
        <div>
          <h1 style="font-size: 1.3rem; font-weight: 700; color: var(--text); margin-bottom: 2px;">{{ job.title }}</h1>
          <p style="font-size: 13px; color: var(--text-2);">{{ job.company }}{% if job.location %} · {{ job.location }}{% endif %}</p>
        </div>
      </div>
      <span class="badge {% if job.job_type == 'remote' %}badge-green{% elif job.job_type == 'internship' %}badge-purple{% elif job.job_type == 'freelance' %}badge-yellow{% else %}badge-blue{% endif %}" style="flex-shrink: 0;">{{ job.get_job_type_display }}</span>
    </div>

    <hr>

    <div style="margin: 16px 0;">
      <p style="font-size: 11px; font-weight: 600; color: var(--text-3); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 8px;">About the Role</p>
      <p style="font-size: 14px; color: var(--text-2); line-height: 1.7; white-space: pre-line;">{{ job.description }}</p>
    </div>

    {% if job.requirements %}
    <div style="margin: 16px 0;">
      <p style="font-size: 11px; font-weight: 600; color: var(--text-3); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 8px;">Requirements</p>
      <p style="font-size: 14px; color: var(--text-2); line-height: 1.7; white-space: pre-line;">{{ job.requirements }}</p>
    </div>
    {% endif %}

    <hr>

    <div style="display: flex; align-items: center; justify-content: space-between; margin-top: 16px;">
      <p style="font-size: 12px; color: var(--text-3);">Posted by {{ job.poster.username }} · {{ job.created_at|timesince }} ago</p>

      {% if job.apply_link %}
      <a href="{{ job.apply_link }}" target="_blank" class="btn btn-primary">Apply Now ↗</a>
      {% elif user.is_authenticated %}
        {% if already_applied %}
        <span class="badge badge-green" style="padding: 8px 16px;">✓ Applied</span>
        {% elif user != job.poster %}
        <button onclick="document.getElementById('apply-form').style.display='block'; this.style.display='none'" class="btn btn-primary">Apply Now</button>
        {% endif %}
      {% else %}
      <a href="{% url 'login' %}" class="btn btn-outline">Log in to Apply</a>
      {% endif %}
    </div>

    <!-- INLINE APPLY FORM -->
    {% if user.is_authenticated and not already_applied and user != job.poster and not job.apply_link %}
    <div id="apply-form" style="display: none; margin-top: 16px; padding-top: 16px; border-top: 1px solid var(--border);">
      <form method="post" action="{% url 'job_apply' job.pk %}">
        {% csrf_token %}
        <label class="form-label">Cover Letter (optional)</label>
        <textarea name="cover_letter" class="form-textarea" rows="4" placeholder="Tell them why you're a great fit..." style="resize: vertical; margin-bottom: 10px;"></textarea>
        <button type="submit" class="btn btn-primary">Submit Application</button>
      </form>
    </div>
    {% endif %}
  </div>
</div>
{% endblock %}